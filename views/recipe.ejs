<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Recipe App</title>
    <style>
        body {
            background-color: #B8A38C;
            color: #333;
        }

        .navbar {
            background-color: #315b46;
        }

        .navbar-brand,
        .nav-link {
            color: #B8A38C !important;
            font-family: monospace;
        }

        .container {
            background-color: #917d66;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h2 {
            color: #315b46;
        }

        p {
            font-size: 1.1em;
        }

        .btn-primary {
            background-color: #814520;
            border: none;
        }

        .btn-primary:hover {
            background-color: #693615;
        }

        .recipe-detail {
            margin-bottom: 20px;
        }

        .recipe-detail img,
        .recipe-detail video {
            max-width: 100%;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .recipe-info {
            background-color: #B8A38C;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-sm">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Recipe App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/addRecipe">Add new recipes</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="text-center mb-4">
            <h2>Recipe Details</h2>
        </div>
        <% if (recipe) { %>
            <div class="recipe-detail">
                <div class="recipe-info">
                    <p><b>Recipe Name:</b><br>
                        <%= recipe.name %>
                    </p>
                    <p><b>Recipe Image:</b><br><img src="/images/<%= recipe.image %>" alt="Recipe Image" height="450"></p>
                    <% if (recipe.video) { %>
                        <p><b>Recipe Video:</b><br><video controls>
                                <source src="/videos/<%= recipe.video %>" type="video/mp4">
                            </video></p>
                    <% } else { %>
                        <p><b>Recipe Video:</b><br>No video uploaded.</p>
                    <% } %>
                    <p><b>Recipe Ingredients:</b><br>
                        <%= recipe.ingrediant %>
                    </p>
                    <p><b>Recipe Instructions:</b><br>
                        <%= recipe.instructions %>
                    </p>
                    <p><b>Recipe Nutrition:</b><br>
                        <%= recipe.nutrition %>
                    </p>
                    <p><b>Total Time:</b><br>
                        <%= recipe.time %>
                    </p>
                    <p><b>Author:</b><br>
                        <%= recipe.author %>
                    </p>
                </div>
            </div>
        <% } else { %>
            <p>No recipe found.</p>
        <% } %>
        <a href="/" class="btn btn-primary">Back</a>
        <a href="/editRecipe/<%= recipe.id %>" class="btn btn-primary">Edit</a>
        <a href="/deleteRecipe/<%= recipe.id %>" class="btn btn-primary" onclick="return comfirm('Are you sure you want to delete this product?')">Delete</a>
    </div>

</body>

</html>
